---
# Replace this example or create the Secret with kubectl; do not commit a real token.
apiVersion: v1
kind: Secret
metadata:
  name: hetdns-token
  labels:
    app.kubernetes.io/name: hetdns
type: Opaque
stringData:
  token: REPLACE_WITH_HETZNER_CLOUD_TOKEN
