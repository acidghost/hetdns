apiVersion: v1
kind: Service
metadata:
  name: {{ include "hetdns.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "hetdns.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "hetdns.selectorLabels" . | nindent 4 }}
